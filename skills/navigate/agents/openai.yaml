interface:
  display_name: "Navigate Lightfast"
  short_description: "Find or advance one live Lightfast route"
  default_prompt: "Use $navigate to find or advance one live Lightfast route for this request."
policy:
  allow_implicit_invocation: true
