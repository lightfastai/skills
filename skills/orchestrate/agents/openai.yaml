interface:
  display_name: "Orchestrate"
  short_description: "Schedule and delegate one repository programme"
  default_prompt: "Use $orchestrate to recover the current programme, select its valid delivery frontier, and return one bounded Ask Matt delegation without implementing in the root task."
