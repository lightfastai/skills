interface:
  display_name: "Orchestrate"
  short_description: "Recover and coordinate one repository programme"
  default_prompt: "Use $orchestrate to recover the current programme from durable evidence and return its next bounded coordination action without implementing in the root task."
